# Scripts

This folder is reserved for utility scripts (e.g., data fetching, preprocessing helpers).

- Keep sensitive data and large files out of the repo.
- Prefer configuration via environment variables.
